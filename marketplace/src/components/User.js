import React from 'react'

const User = props => {
    console.log(props)
    return (
        <h3>User data heading</h3>
    )

const mapStateToProps = state => {
    return{
      // state this component needs
    }
  }
}
export default User